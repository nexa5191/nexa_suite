import { LeaveConfigClient } from "@/components/hr/leave-config-client";

export const metadata = { title: "Leave Policy — NEXA" };

export default function LeaveConfigPage() {
  return <LeaveConfigClient />;
}
