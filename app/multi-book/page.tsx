import { MultiBookClient } from "@/components/accounting/multi-book-client";

export const metadata = { title: "Multi-Book Ledger — NEXA" };

export default function MultiBookPage() {
  return <MultiBookClient />;
}
