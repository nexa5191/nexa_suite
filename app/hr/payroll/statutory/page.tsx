import { StatutoryClient } from "@/components/hr/statutory-client";

export const metadata = { title: "Payroll Statutory — NEXA" };

export default function StatutoryPage() {
  return <StatutoryClient />;
}
