import { AttendanceClient } from "@/components/hr/attendance-client";

export const metadata = { title: "Attendance — NEXA" };

export default function AttendancePage() {
  return <AttendanceClient />;
}
