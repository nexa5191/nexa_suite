import { OnboardingClient } from "@/components/hr/onboarding-client";

export const metadata = { title: "Onboarding & Offboarding — NEXA" };

export default function OnboardingPage() {
  return <OnboardingClient />;
}
