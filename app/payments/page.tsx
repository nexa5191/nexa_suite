import { PaymentsClient } from "@/components/finance/payments-client";

export const metadata = { title: "Payment Runs — NEXA" };

export default function PaymentsPage() {
  return <PaymentsClient />;
}
