import { AuditTrailClient } from "@/components/audit/audit-trail-client";

export const metadata = { title: "Audit Trail — NEXA" };

export default function AuditTrailPage() {
  return <AuditTrailClient />;
}
