import { DocumentsClient } from "@/components/documents/documents-client";

export const metadata = { title: "Documents — NEXA" };

export default function DocumentsPage() {
  return <DocumentsClient />;
}
