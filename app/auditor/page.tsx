import { AuditorPortalClient } from "@/components/auditor/auditor-portal-client";

export const metadata = { title: "Auditor Portal — NEXA" };

export default function AuditorPage() {
  return <AuditorPortalClient />;
}
