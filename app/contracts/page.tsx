import { ContractsClient } from "@/components/finance/contracts-client";

export const metadata = { title: "Contracts & AMCs — NEXA" };

export default function ContractsPage() {
  return <ContractsClient />;
}
