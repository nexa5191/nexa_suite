import { ReturnsClient } from "@/components/orders/returns-client";

export const metadata = { title: "Customer Returns — NEXA" };

export default function ReturnsPage() {
  return <ReturnsClient />;
}
