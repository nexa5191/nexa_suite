import { StockCountClient } from "@/components/inventory/stock-count-client";

export const metadata = { title: "Stock Count — NEXA" };

export default function StockCountPage() {
  return <StockCountClient />;
}
