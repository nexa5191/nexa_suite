import { InventoryClient } from "@/components/inventory/inventory-client";

export const metadata = { title: "Inventory — NEXA" };

export default function InventoryPage() {
  return <InventoryClient />;
}
