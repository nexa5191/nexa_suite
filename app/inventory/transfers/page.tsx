import { TransfersClient } from "@/components/inventory/transfers-client";

export const metadata = { title: "Inter-warehouse Transfers — NEXA" };

export default function TransfersPage() {
  return <TransfersClient />;
}
