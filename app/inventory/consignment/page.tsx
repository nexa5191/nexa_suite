import { ConsignmentClient } from "@/components/inventory/consignment-client";

export const metadata = { title: "Consignment / VMI" };

export default function Page() {
  return <ConsignmentClient />;
}
