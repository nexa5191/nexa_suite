import { PutawayClient } from "@/components/inventory/putaway-client";

export const metadata = { title: "Put-away & Picking — NEXA" };

export default function PutawayPage() {
  return <PutawayClient />;
}
