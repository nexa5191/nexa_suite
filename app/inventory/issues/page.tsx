import { IssuesClient } from "@/components/inventory/issues-client";

export const metadata = { title: "Material Issues — NEXA" };

export default function IssuesPage() {
  return <IssuesClient />;
}
