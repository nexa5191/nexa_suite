import { CostingClient } from "@/components/inventory/costing-client";

export const metadata = { title: "Inventory Costing — NEXA" };

export default function CostingPage() {
  return <CostingClient />;
}
