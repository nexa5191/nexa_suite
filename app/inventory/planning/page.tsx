import { PlanningClient } from "@/components/inventory/planning-client";

export const metadata = { title: "Demand Planning — NEXA" };

export default function PlanningPage() {
  return <PlanningClient />;
}
