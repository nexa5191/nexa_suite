import { ReorderClient } from "@/components/inventory/reorder-client";

export const metadata = { title: "Reorder Alerts — NEXA" };

export default function ReorderPage() {
  return <ReorderClient />;
}
