import { TimesheetsClient } from "@/components/timesheets/timesheets-client";

export const metadata = { title: "Timesheets — NEXA" };

export default function Page() {
  return <TimesheetsClient />;
}
