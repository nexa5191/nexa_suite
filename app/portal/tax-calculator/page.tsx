import { TaxCalculatorClient } from "@/components/hr/tax-calculator-client";

export const metadata = { title: "Income Tax Calculator — NEXA" };

export default function TaxCalculatorPage() {
  return <TaxCalculatorClient />;
}
