import { GstRegistersClient } from "@/components/tax/gst-registers-client";

export const metadata = { title: "GST Registers — NEXA" };

export default function GstRegistersPage() {
  return <GstRegistersClient />;
}
