import { ReportHubClient } from "@/components/reports/report-hub-client";

export const metadata = { title: "Report Explorer — NEXA" };

export default function ReportExplorerPage() {
  return <ReportHubClient />;
}
