import { CashflowForecastClient } from "@/components/reports/cashflow-forecast-client";

export const metadata = { title: "Projected Cash Flow — NEXA" };

export default function CashflowForecastPage() {
  return <CashflowForecastClient />;
}
