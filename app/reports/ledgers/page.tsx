import { PartyLedgerClient } from "@/components/reports/party-ledger-client";

export const metadata = { title: "Party Ledger — NEXA" };

export default function PartyLedgerPage() {
  return <PartyLedgerClient />;
}
