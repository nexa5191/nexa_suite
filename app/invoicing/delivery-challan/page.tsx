import { DeliveryChallanClient } from "@/components/invoicing/delivery-challan-client";

export const metadata = { title: "Delivery Challans — NEXA" };

export default function DeliveryChallanPage() {
  return <DeliveryChallanClient />;
}
