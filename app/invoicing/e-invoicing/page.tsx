import { EInvoiceClient } from "@/components/tax/einvoice-client";

export const metadata = { title: "e-Invoicing — NEXA" };

export default function EInvoicingPage() {
  return <EInvoiceClient />;
}
