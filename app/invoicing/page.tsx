import { InvoicingClient } from "@/components/invoicing/invoicing-client";

export const metadata = { title: "Invoicing — NEXA" };

export default function InvoicingPage() {
  return <InvoicingClient />;
}
