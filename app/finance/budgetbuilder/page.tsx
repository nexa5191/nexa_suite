import { BudgetBuilderClient } from "@/components/finance/budget-builder-client";

export const metadata = { title: "Budget Builder — NEXA" };

export default function BudgetBuilderPage() {
  return <BudgetBuilderClient />;
}
